source "$HOME/.envrc"

# The client harness `just proof-integrated` probes: dev-server/run-harness.mjs in the
# client checkout serves the annotated test documents here. No default stands behind it,
# so it is declared rather than guessed.
export ANNOTATE_HARNESS_URL="http://localhost:3012"
